#!/usr/bin/env python

from distutils.core import setup

setup(name='src',
      version='1.0',
      description='Fake',
      author='Fake',
      author_email='fake@fake.com',
      url='https://fake.com',
      packages=['preproc', 'eda', 'embedding', 'models']
     )
